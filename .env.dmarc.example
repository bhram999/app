# DMARC Parser Configuration
# Copy this file to .env.dmarc and fill in your credentials
# DO NOT commit .env.dmarc to version control!

# Gmail credentials
GMAIL_EMAIL=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password

# Optional: IMAP server (default: imap.gmail.com)
# IMAP_SERVER=imap.gmail.com

# Optional: Mailbox to search (default: INBOX)
# MAILBOX=INBOX

# Optional: Number of emails to process (default: 50)
# LIMIT=50

# Optional: Output file path
# OUTPUT_FILE=dmarc_failures.json
